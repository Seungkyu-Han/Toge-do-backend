name: Gateway서버의 CI/CD 설정

on:
  push:
    branches: [ "gateway" ]
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@af1da67850ed9a4cedd57bfd976089dd991e2582

    - name: Grant execute permission for gradlew
      run: chmod +x ./gradlew

    - name: Build with Gradle Wrapper
      run: ./gradlew :gateway:build

    - name: Debug Source Directory
      run: ls -al gateway/presentation
      
    - name: Scp to EC2
      uses: appleboy/scp-action@master
      with:
        host: ${{secrets.SERVER_IP}}
        username: ${{secrets.SSH_USER}}
        key: ${{secrets.SSH_PRIVATE_KEY}}
        source: |
          gateway/presentation/build/libs/presentation.jar
        target: gateway/
        strip_components: 3
      
  # deploy:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Setup SSH
  #       uses: webfactory/ssh-agent@v0.5.4
  #       with:
  #         ssh-private-key: ${{secrets.SSH_PRIVATE_KEY}}
          
  #     - name: Deploy Docker Container
  #       run: |
  #         ssh -o StrictHostKeyChecking=no ${{secrets.SSH_USER}}@${{secrets.SERVER_IP}} << 'EOF'
  #         cd was
  #         docker stop {컨테이너 이름} && docker rm {컨테이너 이름} && docker rmi {이미지 이름}:latest
  #         docker build -t {이미지 이름} .
  #         docker-compose up -d
  #         EOF
